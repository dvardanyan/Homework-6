function allStr(str,n) {
    let arr = [];
	for (let i = 0;i<str.length;i++) {
           arr.push(str.substring(i,i+n))
	  }
	let ans = arr.filter(a=>a.length==n)
   
	return ans;
}


let str = "abcdfghz", n = 7;
